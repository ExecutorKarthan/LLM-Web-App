{
  "name": "llm-web-app",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && venv\\Scripts\\activate && python manage.py runserver",
    "client": "cd frontend && npm run dev",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
