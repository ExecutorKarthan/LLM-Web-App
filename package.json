{
  "name": "llm-web-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm install && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "backend": "cd backend && python manage.py runserver 0.0.0.0:8000",
    "dev": "vite", 
    "build": "cd frontend && npm install && npm run build && cd ../backend && python manage.py collectstatic --noinput",
    "start": "npm run build && cd backend && gunicorn mysite.wsgi:application --bind 0.0.0.0:32775"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
