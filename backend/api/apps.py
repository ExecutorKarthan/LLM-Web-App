# Imports needed modules
from django.apps import AppConfig

# Defines the class for the api configuration
class ApiConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'api'
